export default function RequestRenderedPage() {
  return (
    <main>
      <h1>Request-rendered fixture</h1>
      <p>This fixed App Router route is rendered per request.</p>
      <div className="card">Screenshot this route at every configured viewport.</div>
    </main>
  );
}
